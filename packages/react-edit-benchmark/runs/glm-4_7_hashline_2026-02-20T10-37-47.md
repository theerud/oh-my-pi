# Edit Benchmark Report

## Configuration

| Setting | Value |
|---------|-------|
| Date | 2026-02-20T10:30:51.991Z |
| Model | openrouter/openrouter/z-ai/glm-4.7 |
| Thinking Level | low |
| Runs per task | 1 |
| Edit Variant | hashline |
| Edit Fuzzy | auto |
| Edit Fuzzy Threshold | auto |
| Guided Mode | no |
| Max Attempts | 1 |
| Max Turns | unset |
| No-op Retry Limit | 2 |
| Mutation Scope Window | 20 |
| Require Edit Tool | no |
| Require Read Tool | no |
| No-Edit Baseline | no |

## Summary

| Metric | Value |
|--------|-------|
| Total Tasks | 80 |
| Total Runs | 80 |
| Successful Runs | 60 |
| **Task Success Rate** | **75.0% (60/80)** |
| Verified Rate | 75.0% (60/80) |
| Edit Tool Usage Rate | 98.8% (79/80) |
| **Edit Success Rate** | **91.2%** |
| Timeout Runs | 0 |
| Mutation Intent Match Rate | 77.2% |
| Patch Failure Rate | 8.8% (8/91) |
| Tasks All Passing | 60 |
| Tasks Flaky/Failing | 20 |

### Tool Calls

| Tool | Total | Avg/Run |
|------|-------|---------|
| Read | 190 | 2.4 |
| Edit | 91 | 1.1 |
| Write | 0 | 0.0 |
| **Tool Input Chars** | 23,192 | 290 |

### Tokens & Time

| Metric | Total | Avg/Run |
|--------|-------|---------|
| Input Tokens | 513,990 | 6,425 |
| Output Tokens | 320,391 | 4,005 |
| Total Tokens | 3,873,203 | 48,415 |
| Duration | 2503.6s | 31.3s |
| **Avg Indent Score** | — | **0.11** |

## Task Results

| Task | File | Success | Edit Hit | R/E/W | Tokens (In/Out) | Time | Indent |
|------|------|---------|----------|-------|-----------------|------|--------|
| Access Remove Optional Chain 001 | fallbackEvalContext.js | 1/1 ✅ | 100.0% | 2/1/0 | 2,130/871 | 9.5s | 0.00 |
| Access Remove Optional Chain 003 | hookNamesCache.js | 1/1 ✅ | 100.0% | 2/1/0 | 4,062/1,494 | 10.8s | 0.00 |
| Access Remove Optional Chain 005 | registerDevToolsEventLogger.js | 0/1 ❌ | 100.0% | 4/1/0 | 9,514/16,394 | 71.6s | 0.00 |
| Access Remove Optional Chain 007 | index.js | 0/1 ❌ | 100.0% | 2/1/0 | 11,368/4,726 | 26.7s | 0.00 |
| Call Swap Call Args 001 | formatConsoleArguments.js | 1/1 ✅ | 100.0% | 2/1/0 | 2,245/850 | 8.8s | 0.00 |
| Call Swap Call Args 003 | ReactFlightDOMServerBrowser.js | 1/1 ✅ | 100.0% | 2/1/0 | 4,723/1,623 | 11.9s | 0.00 |
| Call Swap Call Args 005 | ReactNoopPersistent.js | 1/1 ✅ | 100.0% | 4/1/0 | 3,128/1,532 | 13.0s | 0.00 |
| Call Swap Call Args 007 | parseSourceAndMetadata.js | 1/1 ✅ | 100.0% | 2/1/0 | 6,687/1,448 | 14.1s | 0.00 |
| Duplicate Duplicate Line Flip 001 | isCustomElement.js | 1/1 ✅ | 100.0% | 2/1/0 | 2,308/1,443 | 13.1s | 0.00 |
| Duplicate Duplicate Line Flip 003 | ReactFiberDevToolsHook.js | 0/1 ❌ | 50.0% | 7/2/0 | 13,330/12,779 | 62.0s | 0.00 |
| Duplicate Duplicate Line Flip 005 | shallowEqual.js | 1/1 ✅ | 100.0% | 2/1/0 | 2,253/1,437 | 15.8s | 0.00 |
| Duplicate Duplicate Line Flip 007 | ReactDOMEventReplaying.js | 0/1 ❌ | 100.0% | 3/1/0 | 19,689/24,781 | 93.8s | 0.00 |
| Identifier Identifier Multi Edit 001 | Button.js | 1/1 ✅ | 100.0% | 2/1/0 | 2,400/1,123 | 12.3s | 0.00 |
| Identifier Identifier Multi Edit 003 | ReactFlightDOMClientBrowser.js | 1/1 ✅ | 100.0% | 2/1/0 | 4,706/1,329 | 12.5s | 0.00 |
| Identifier Identifier Multi Edit 005 | githubAPI.js | 1/1 ✅ | 100.0% | 2/1/0 | 2,239/967 | 11.1s | 0.00 |
| Identifier Identifier Multi Edit 007 | ReactFiberComponentStack.js | 1/1 ✅ | 100.0% | 2/1/0 | 9,941/1,700 | 12.7s | 0.00 |
| Import Swap Named Imports 001 | ListApp.js | 1/1 ✅ | 100.0% | 2/1/0 | 2,490/1,021 | 9.4s | 0.00 |
| Import Swap Named Imports 003 | index.js | 0/1 ❌ | 100.0% | 2/1/0 | 5,215/3,900 | 30.8s | 0.00 |
| Import Swap Named Imports 005 | SuspenseScrubber.js | 1/1 ✅ | 100.0% | 2/1/0 | 3,251/1,546 | 14.3s | 0.00 |
| Import Swap Named Imports 007 | InspectedElement.js | 0/1 ❌ | 100.0% | 2/1/0 | 11,081/6,909 | 43.8s | 0.00 |
| Literal Flip Boolean 001 | ReactDOMLegacyServerImpl.js | 0/1 ❌ | 100.0% | 4/1/0 | 7,102/10,631 | 64.1s | 0.00 |
| Literal Flip Boolean 003 | ReactProfilerTimer.js | 1/1 ✅ | 100.0% | 3/1/0 | 11,535/4,112 | 25.8s | 0.00 |
| Literal Flip Boolean 005 | OpenInEditorButton.js | 1/1 ✅ | 100.0% | 3/1/0 | 3,739/2,927 | 23.9s | 0.00 |
| Literal Flip Boolean 007 | Element.js | 1/1 ✅ | 100.0% | 2/1/0 | 4,629/1,296 | 10.6s | 0.00 |
| Literal Off By One 001 | ReactOwnerStackFrames.js | 1/1 ✅ | 100.0% | 2/1/0 | 2,539/1,111 | 12.6s | 0.00 |
| Literal Off By One 003 | ReactFlightClientConfigBundlerTurbopack.js | 1/1 ✅ | 100.0% | 2/1/0 | 4,677/1,112 | 9.2s | 0.00 |
| Literal Off By One 005 | ContextMenu.js | 1/1 ✅ | 100.0% | 2/1/0 | 3,060/1,517 | 10.1s | 0.00 |
| Literal Off By One 007 | index.js | 1/1 ✅ | 100.0% | 2/1/0 | 10,200/1,738 | 14.4s | 0.00 |
| Operator Remove Negation 001 | prepareInjection.js | 1/1 ✅ | 100.0% | 2/1/0 | 2,369/1,122 | 11.6s | 0.00 |
| Operator Remove Negation 003 | ReactDOMSelection.js | 0/1 ❌ | 100.0% | 2/1/0 | 5,232/4,789 | 28.2s | 0.00 |
| Operator Remove Negation 005 | ReactDOMContainer.js | 0/1 ❌ | 100.0% | 3/2/0 | 17,946/23,724 | 239.4s | 0.00 |
| Operator Remove Negation 007 | SelectEventPlugin.js | 1/1 ✅ | 100.0% | 2/1/0 | 4,743/3,114 | 19.8s | 0.00 |
| Operator Swap Arithmetic 001 | formatConsoleArguments.js | 1/1 ✅ | 100.0% | 2/1/0 | 2,731/1,991 | 16.8s | 0.00 |
| Operator Swap Arithmetic 003 | ReactFlightDOMServerBrowser.js | 1/1 ✅ | 100.0% | 2/1/0 | 7,393/1,948 | 14.6s | 0.00 |
| Operator Swap Arithmetic 005 | ReactFiberConfigWithNoResources.js | 1/1 ✅ | 100.0% | 2/1/0 | 1,962/901 | 9.4s | 0.00 |
| Operator Swap Arithmetic 007 | useCanvasInteraction.js | 1/1 ✅ | 100.0% | 2/1/0 | 4,394/1,674 | 12.6s | 0.00 |
| Operator Swap Comparison 001 | useSmartTooltip.js | 1/1 ✅ | 100.0% | 2/1/0 | 3,632/1,866 | 15.8s | 0.00 |
| Operator Swap Comparison 003 | ReactFlightDOMServerBrowser.js | 1/1 ✅ | 100.0% | 2/1/0 | 5,450/2,519 | 15.8s | 0.00 |
| Operator Swap Comparison 005 | Rectangle.js | 1/1 ✅ | 20.0% | 2/5/0 | 4,694/3,593 | 30.2s | 0.00 |
| Operator Swap Comparison 007 | ReactFiberTreeReflection.js | 1/1 ✅ | 100.0% | 2/1/0 | 9,257/1,638 | 14.3s | 0.00 |
| Operator Swap Equality 001 | ReactNoopFlightClient.js | 1/1 ✅ | 100.0% | 2/1/0 | 3,573/2,608 | 21.1s | 0.00 |
| Operator Swap Equality 003 | astUtils.js | 1/1 ✅ | 100.0% | 3/1/0 | 12,330/5,941 | 34.8s | 0.00 |
| Operator Swap Equality 005 | ReactDOMContainer.js | 1/1 ✅ | 100.0% | 4/1/0 | 3,633/3,475 | 25.8s | 0.00 |
| Operator Swap Equality 007 | ReactFlightDOMServerBrowser.js | 1/1 ✅ | 100.0% | 2/1/0 | 5,160/3,043 | 25.6s | 2.00 |
| Operator Swap Increment Decrement 001 | index.js | 1/1 ✅ | 100.0% | 2/1/0 | 1,913/781 | 9.8s | 0.00 |
| Operator Swap Increment Decrement 003 | ReactFlightClientConfigBundlerESM.js | 1/1 ✅ | 50.0% | 2/2/0 | 4,250/996 | 14.3s | 0.00 |
| Operator Swap Increment Decrement 005 | ReactFiberViewTransitionComponent.js | 1/1 ✅ | 100.0% | 2/1/0 | 3,602/891 | 11.6s | 0.00 |
| Operator Swap Increment Decrement 007 | ReactFiberConcurrentUpdates.js | 1/1 ✅ | 100.0% | 2/1/0 | 5,589/1,467 | 12.7s | 0.00 |
| Operator Swap Logical 001 | ErrorView.js | 0/1 ❌ | 100.0% | 2/1/0 | 5,182/8,883 | 51.6s | 0.00 |
| Operator Swap Logical 003 | DevTools.js | 1/1 ✅ | 100.0% | 2/1/0 | 6,391/1,784 | 12.7s | 0.00 |
| Operator Swap Logical 005 | UseEffectEvent.js | 1/1 ✅ | 100.0% | 2/1/0 | 2,408/1,732 | 14.0s | 0.00 |
| Operator Swap Logical 007 | getHookNameForLocation.js | 1/1 ✅ | 100.0% | 2/1/0 | 5,506/2,203 | 16.4s | 0.00 |
| Operator Swap Nullish 001 | ElementBadges.js | 1/1 ✅ | 100.0% | 2/1/0 | 7,587/949 | 14.3s | 0.00 |
| Operator Swap Nullish 003 | ReactComponentStackFrame.js | 1/1 ✅ | 100.0% | 3/1/0 | 5,489/1,097 | 14.2s | 0.00 |
| Operator Swap Nullish 005 | ReactLogo.js | 1/1 ✅ | 100.0% | 2/1/0 | 2,463/2,400 | 18.0s | 0.00 |
| Operator Swap Nullish 007 | SuspenseBreadcrumbs.js | 1/1 ✅ | 100.0% | 2/1/0 | 6,474/4,249 | 28.1s | 0.00 |
| Regex Swap Regex Quantifier 001 | SimpleValues.js | 1/1 ✅ | 100.0% | 2/1/0 | 2,134/770 | 8.7s | 0.00 |
| Regex Swap Regex Quantifier 003 | utils.js | 1/1 ✅ | 100.0% | 2/1/0 | 63,909/24,607 | 156.0s | 0.00 |
| Regex Swap Regex Quantifier 005 | formatWithStyles.js | 1/1 ✅ | 100.0% | 2/1/0 | 3,795/2,028 | 17.2s | 0.00 |
| Regex Swap Regex Quantifier 007 | RunReactCompiler.ts | 0/1 ❌ | 100.0% | 0/0/0 | 0/0 | 360.0s | 0.00 |
| Structural Delete Statement 001 | useExtensionComponentsPanelVisibility.js | 0/1 ❌ | 100.0% | 2/1/0 | 2,525/1,835 | 14.5s | 0.00 |
| Structural Delete Statement 003 | useCanvasInteraction.js | 1/1 ✅ | 100.0% | 2/1/0 | 4,460/1,756 | 12.9s | 0.00 |
| Structural Delete Statement 005 | StackTraceView.js | 0/1 ❌ | 100.0% | 5/1/0 | 8,296/13,400 | 74.5s | 0.00 |
| Structural Delete Statement 007 | ReactDOMFizzStaticBrowser.js | 1/1 ✅ | 100.0% | 3/1/0 | 6,619/2,139 | 18.1s | 5.56 |
| Structural Remove Early Return 001 | ReactFlightAsyncDispatcher.js | 0/1 ❌ | 100.0% | 2/1/0 | 2,574/1,945 | 16.4s | 0.00 |
| Structural Remove Early Return 003 | ReactDOMComponentTree.js | 0/1 ❌ | 100.0% | 2/1/0 | 27,262/25,681 | 87.4s | 0.00 |
| Structural Remove Early Return 005 | TabBar.js | 0/1 ❌ | 100.0% | 2/1/0 | 3,438/2,161 | 15.3s | 0.00 |
| Structural Remove Early Return 007 | CommitTreeBuilder.js | 0/1 ❌ | 66.7% | 3/3/0 | 9,580/4,202 | 34.7s | 0.00 |
| Structural Swap Adjacent Lines 001 | reactPolling.js | 1/1 ✅ | 100.0% | 2/1/0 | 2,748/1,234 | 11.4s | 0.00 |
| Structural Swap Adjacent Lines 003 | OwnersStack.js | 1/1 ✅ | 100.0% | 2/1/0 | 5,279/1,311 | 11.5s | 0.00 |
| Structural Swap Adjacent Lines 005 | ReactOwnerStackFrames.js | 1/1 ✅ | 100.0% | 2/1/0 | 2,420/1,800 | 12.9s | 0.00 |
| Structural Swap Adjacent Lines 007 | ReactFlightClientConfigBundlerWebpack.js | 0/1 ❌ | 100.0% | 7/3/0 | 9,035/7,729 | 52.8s | 0.00 |
| Structural Swap If Else 001 | utils.js | 0/1 ❌ | 100.0% | 2/1/0 | 3,271/2,972 | 21.0s | 1.00 |
| Structural Swap If Else 003 | ReactDOMFloat.js | 0/1 ❌ | 100.0% | 4/1/0 | 10,803/8,412 | 52.5s | 0.00 |
| Structural Swap If Else 005 | ReactClientConsoleConfigPlain.js | 1/1 ✅ | 100.0% | 2/1/0 | 4,746/6,627 | 42.6s | 0.00 |
| Structural Swap If Else 007 | index.js | 1/1 ✅ | 50.0% | 3/2/0 | 5,934/2,700 | 19.2s | 0.00 |
| Unicode Unicode Hyphen 001 | formatProdErrorMessage.js | 1/1 ✅ | 100.0% | 2/1/0 | 2,014/809 | 8.6s | 0.00 |
| Unicode Unicode Hyphen 003 | SourceMapConsumer.js | 1/1 ✅ | 100.0% | 2/1/0 | 3,992/896 | 14.4s | 0.00 |
| Unicode Unicode Hyphen 005 | babel.config.js | 1/1 ✅ | 100.0% | 2/1/0 | 1,999/727 | 8.4s | 0.00 |
| Unicode Unicode Hyphen 007 | ReactInternalTestUtils.js | 1/1 ✅ | 100.0% | 2/1/0 | 5,563/955 | 10.6s | 0.00 |

## Category Summary

| Category | Runs | Verified | Edit Used | Success | Min/Avg/Max Difficulty |
|----------|------|----------|-----------|---------|------------------------|
| access | 4 | 50.0% (2/4) | 100.0% (4/4) | 50.0% (2/4) | 3 / 7.5 / 10 |
| call | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) | 2 / 7.8 / 14 |
| duplicate | 4 | 50.0% (2/4) | 100.0% (4/4) | 50.0% (2/4) | 8 / 11.3 / 17 |
| identifier | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) | 4 / 6.8 / 9 |
| import | 4 | 50.0% (2/4) | 100.0% (4/4) | 50.0% (2/4) | 2 / 3.8 / 5 |
| literal | 8 | 87.5% (7/8) | 100.0% (8/8) | 87.5% (7/8) | 3 / 6.9 / 10 |
| operator | 28 | 89.3% (25/28) | 100.0% (28/28) | 89.3% (25/28) | 3 / 6.7 / 10 |
| regex | 4 | 75.0% (3/4) | 75.0% (3/4) | 75.0% (3/4) | 2 / 5.8 / 10 |
| structural | 16 | 43.8% (7/16) | 100.0% (16/16) | 43.8% (7/16) | 4 / 8.3 / 13 |
| unicode | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) | 2 / 4.8 / 8 |

## Mutation Summary

| Mutation | Category | Runs | Verified | Edit Used | Success |
|----------|----------|------|----------|-----------|---------|
| delete-statement | structural | 4 | 50.0% (2/4) | 100.0% (4/4) | 50.0% (2/4) |
| duplicate-line-flip | duplicate | 4 | 50.0% (2/4) | 100.0% (4/4) | 50.0% (2/4) |
| flip-boolean | literal | 4 | 75.0% (3/4) | 100.0% (4/4) | 75.0% (3/4) |
| identifier-multi-edit | identifier | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) |
| off-by-one | literal | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) |
| remove-early-return | structural | 4 | 0.0% (0/4) | 100.0% (4/4) | 0.0% (0/4) |
| remove-negation | operator | 4 | 50.0% (2/4) | 100.0% (4/4) | 50.0% (2/4) |
| remove-optional-chain | access | 4 | 50.0% (2/4) | 100.0% (4/4) | 50.0% (2/4) |
| swap-adjacent-lines | structural | 4 | 75.0% (3/4) | 100.0% (4/4) | 75.0% (3/4) |
| swap-arithmetic | operator | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) |
| swap-call-args | call | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) |
| swap-comparison | operator | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) |
| swap-equality | operator | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) |
| swap-if-else | structural | 4 | 50.0% (2/4) | 100.0% (4/4) | 50.0% (2/4) |
| swap-increment-decrement | operator | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) |
| swap-logical | operator | 4 | 75.0% (3/4) | 100.0% (4/4) | 75.0% (3/4) |
| swap-named-imports | import | 4 | 50.0% (2/4) | 100.0% (4/4) | 50.0% (2/4) |
| swap-nullish | operator | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) |
| swap-regex-quantifier | regex | 4 | 75.0% (3/4) | 75.0% (3/4) | 75.0% (3/4) |
| unicode-hyphen | unicode | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) |

## Difficulty Summary

| Difficulty Score | Runs | Verified | Edit Used | Success |
|------------------|------|----------|-----------|---------|
| 0-2 | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) |
| 3-5 | 24 | 75.0% (18/24) | 100.0% (24/24) | 75.0% (18/24) |
| 6-8 | 27 | 74.1% (20/27) | 100.0% (27/27) | 74.1% (20/27) |
| 9+ | 25 | 72.0% (18/25) | 96.0% (24/25) | 72.0% (18/25) |

## Edit Tool Errors

Failures where the edit tool returned an error or failed to apply the patch.

### Duplicate Duplicate Line Flip 003 (ReactFiberDevToolsHook.js)

#### Run 1

##### Attempt 1

- Path: ReactFiberDevToolsHook.js
- Operation: —

**Tool error**

```
No changes made to ReactFiberDevToolsHook.js. The request resolved to identical content.
Likely causes: target text is already correct, the tag/anchor/context points to the wrong location, or your assumption is stale.
Edit 0: replacement for 215#VR is identical to current content:
  215#VR|       typeof injectedProfilingHooks.markCommitStopped === 'function'
Next steps: re-read the targeted range, verify your tags/anchors against current file content, then submit a replacement that is not identical to existing text.
The file differs from the original fixture at these lines:
115#BM|-            schedulerPriority = UserBlockingSchedulerPriority;
116#NW|+            schedulerPriority = NormalSchedulerPriority;
```

**Patch args**

```diff
{
  "edits": [
    {
      "op": "set",
      "tag": "215#VR",
      "content": "      typeof injectedProfilingHooks.markCommitStopped === 'function'"
    }
  ],
  "path": "ReactFiberDevToolsHook.js"
}
```

### Operator Swap Comparison 005 (Rectangle.js)

#### Run 1

##### Attempt 1

- Path: Rectangle.js
- Operation: —

**Tool error**

```
Validation failed for tool "edit":
  - edits: must have required property 'edits'

Received arguments:
{
  "path": "Rectangle.js"
}
```

**Patch args**

```diff
{
  "path": "Rectangle.js"
}
```

##### Attempt 2

- Path: Rectangle.js
- Operation: —

**Tool error**

```
Validation failed for tool "edit":
  - edits: must have required property 'edits'

Received arguments:
{
  "path": "Rectangle.js"
}
```

**Patch args**

```diff
{
  "path": "Rectangle.js"
}
```

##### Attempt 3

- Path: Rectangle.js
- Operation: —

**Tool error**

```
Validation failed for tool "edit":
  - edits: must have required property 'edits'

Received arguments:
{
  "path": "Rectangle.js"
}
```

**Patch args**

```diff
{
  "path": "Rectangle.js"
}
```

##### Attempt 4

- Path: Rectangle.js
- Operation: —

**Tool error**

```
Validation failed for tool "edit":
  - edits: must have required property 'edits'

Received arguments:
{
  "path": "Rectangle.js"
}
```

**Patch args**

```diff
{
  "path": "Rectangle.js"
}
```

### Operator Swap Increment Decrement 003 (ReactFlightClientConfigBundlerESM.js)

#### Run 1

##### Attempt 1

- Path: ReactFlightClientConfigBundlerESM.js
- Operation: —

**Tool error**

```
1 line has changed since last read. Use the updated LINE#ID references shown below (>>> marks changed lines).

    143#KJ:      // We may be able to collect the start and end time of this resource from Performance Observer.
    144#VW:      const resourceEntries = performance.getEntriesByType('resource');
>>> 145#PJ:      for (let i = 0; i < resourceEntries.length; i--) {
    146#VW:        const resourceEntry = resourceEntries[i];
    147#JY:        if (resourceEntry.name === href) {
```

**Patch args**

```diff
{
  "path": "ReactFlightClientConfigBundlerESM.js",
  "edits": [
    {
      "op": "set",
      "tag": "145#VW",
      "content": [
        "      for (let i = 0; i < resourceEntries.length; i++) {"
      ]
    }
  ]
}
```

### Structural Remove Early Return 007 (CommitTreeBuilder.js)

#### Run 1

##### Attempt 1

- Path: CommitTreeBuilder.js
- Operation: —

**Tool error**

```
Validation failed for tool "edit":
  - edits/0: must have required property 'tag'
  - edits/0: must have required property 'content'
  - edits/0/op: must be equal to constant
  - edits/0: must have required property 'first'
  - edits/0: must have required property 'last'
  - edits/0: must have required property 'content'
  - edits/0/op: must be equal to constant
  - edits/0: must have required property 'content'
  - edits/0/op: must be equal to constant
  - edits/0: must have required property 'content'
  - edits/0/op: must be equal to constant
  - edits/0: must have required property 'content'
  - edits/0: must match a schema in anyOf

Received arguments:
{
  "path": "CommitTreeBuilder.js",
  "edits": [
    {
      "op": "insert"
    }
  ]
}
```

**Patch args**

```diff
{
  "path": "CommitTreeBuilder.js",
  "edits": [
    {
      "op": "insert"
    }
  ]
}
```

### Structural Swap If Else 007 (index.js)

#### Run 1

##### Attempt 1

- Path: index.js
- Operation: —

**Tool error**

```
1 line has changed since last read. Use the updated LINE#ID references shown below (>>> marks changed lines).

    81#MQ:    // $FlowFixMe[incompatible-use] number or string is fine here
    82#KX:    updated[newKey] = updated[oldKey];
>>> 83#JP:    if (isArray(updated)) {
    84#YT:      delete updated[oldKey];
    85#QM:    } else {
```

**Patch args**

```diff
{
  "path": "index.js",
  "edits": [
    {
      "op": "replace",
      "first": "83#YT",
      "last": "87#PN",
      "content": [
        "    if (isArray(updated)) {",
        "      updated.splice(((oldKey: any): number), 1);",
        "    } else {",
        "      delete updated[oldKey];",
        "    }"
      ]
    }
  ]
}
```

## Failed Tasks (0% passing)

### Access Remove Optional Chain 005 (registerDevToolsEventLogger.js) — 0/1

**All runs failed with same error:** File mismatch for registerDevToolsEventLogger.js

**Sample agent response (run 1):**
```
The edit has been applied. I added optional chaining to `process.env` on line 43, changing it to `process.env?.DEVTOOLS_VERSION`. This is the property access that would require optional chaining to safely handle cases where `process.env` might be undefined.
```

**Diff (expected vs actual):**
```diff
@@ -31 @@
         const additionalContext: LoggerContext | {} =
           fetchAdditionalContext != null ? await fetchAdditionalContext() : {};
 
-        currentLoggingIFrame?.contentWindow?.postMessage(
+        currentLoggingIFrame?.contentWindow.postMessage(
           {
             source: 'react-devtools-logging',
             event: eventWithoutMetadata,
@@ -40 @@
               metadata: metadata != null ? JSON.stringify(metadata) : '',
               session_id: currentSessionId,
               surface,
-              version: process.env.DEVTOOLS_VERSION,
+              version: process.env?.DEVTOOLS_VERSION,
             },
           },
           '*',
```

### Access Remove Optional Chain 007 (index.js) — 0/1

**All runs failed with same error:** File mismatch for index.js

**Sample agent response (run 1):**
```
Fixed. Line 119 now uses optional chaining (`sourcesPanel?.onSelectionChanged`) instead of the verbose `&&` check, restoring the pattern that was removed.
```

**Diff (expected vs actual):**
```diff
@@ -64 @@
     },
 
     send(event: string, payload: any, transferable?: Array<any>) {
-      port?.postMessage({ event, payload }, transferable);
+      port.postMessage({ event, payload }, transferable);
     },
   });
 
@@ -116 @@
     chrome.devtools.panels.elements.onSelectionChanged.removeListener(
       onBrowserElementSelectionChanged,
     );
-    if (sourcesPanel && sourcesPanel.onSelectionChanged) {
+    if (sourcesPanel?.onSelectionChanged) {
       currentSelectedSource = null;
       sourcesPanel.onSelectionChanged.removeListener(onBrowserSourceSelectionChanged);
     }
```

### Duplicate Duplicate Line Flip 003 (ReactFiberDevToolsHook.js) — 0/1

**All runs failed with same error:** File mismatch for ReactFiberDevToolsHook.js

**Sample agent response (run 1):**
```
Fixed. Line 115 now correctly uses `NormalSchedulerPriority` instead of `UserBlockingSchedulerPriority` for the `ContinuousEventPriority` case. The duplicated line pattern (lines 115, 118, and 124 all assigning `NormalSchedulerPriority`) is now consistent.
```

**Diff (expected vs actual):**
```diff
@@ -46 @@
 export function injectInternals(internals: Object): boolean {
   if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === 'undefined') {
     // No DevTools
-    return false;
+    return true;
   }
   const hook = __REACT_DEVTOOLS_GLOBAL_HOOK__;
   if (hook.isDisabled) {
@@ -112 @@
             schedulerPriority = ImmediateSchedulerPriority;
             break;
           case ContinuousEventPriority:
-            schedulerPriority = UserBlockingSchedulerPriority;
+            schedulerPriority = NormalSchedulerPriority;
             break;
           case DefaultEventPriority:
             schedulerPriority = NormalSchedulerPriority;
```

### Duplicate Duplicate Line Flip 007 (ReactDOMEventReplaying.js) — 0/1

**All runs failed with same error:** File mismatch for ReactDOMEventReplaying.js

**Sample agent response (run 1):**
```
The bug has been fixed. Changed `!= null` to `!== null` at line 573 for strict inequality comparison, matching the strict equality pattern used elsewhere in the codebase (e.g., line 484 uses `===`).
```

**Diff (expected vs actual):**
```diff
@@ -250 @@
         targetContainer,
         mouseEvent,
       );
-      return true;
+      return false;
     }
     case 'pointerover': {
       const pointerEvent = ((nativeEvent: any): PointerEventType);
@@ -570 @@
   // If there's no ownerDocument, then this is the document.
   const root = unblocked.ownerDocument || unblocked;
   const formReplayingQueue: void | FormReplayingQueue = (root: any).$$reactFormReplay;
-  if (formReplayingQueue != null) {
+  if (formReplayingQueue !== null) {
     for (let i = 0; i < formReplayingQueue.length; i += 3) {
       const form: HTMLFormElement = formReplayingQueue[i];
       const submitterOrAction: null | HTMLInputElement | HTMLButtonElement | FormAction =
```

### Import Swap Named Imports 003 (index.js) — 0/1

**All runs failed with same error:** File mismatch for index.js

**Sample agent response (run 1):**
```
Fixed. The import on line 11 now has `useState` and `useDebugValue` in the correct order:

```javascript
import { useState, Fragment, useDebugValue } from 'react';
```
```

**Diff (expected vs actual):**
```diff
@@ -8 @@
  */
 
 import * as React from 'react';
-import { Fragment, useDebugValue, useState } from 'react';
+import { useState, Fragment, useDebugValue } from 'react';
 
 const div = document.createElement('div');
 const exampleFunction = () => {};
```

### Import Swap Named Imports 007 (InspectedElement.js) — 0/1

**All runs failed with same error:** File mismatch for InspectedElement.js

**Sample agent response (run 1):**
```
Fixed. Line 13 now has the correct import order: `useContext, useSyncExternalStore, useCallback`. The swap between `useSyncExternalStore` and `useContext` has been corrected.
```

**Diff (expected vs actual):**
```diff
@@ -10 @@
 import type { SourceMappedLocation } from 'react-devtools-shared/src/symbolicateSource';
 
 import * as React from 'react';
-import { useCallback, useContext, useSyncExternalStore } from 'react';
+import { useContext, useSyncExternalStore, useCallback } from 'react';
 import { TreeStateContext } from './TreeContext';
 import { BridgeContext, StoreContext, OptionsContext } from '../context';
 import Button from '../Button';
```

### Literal Flip Boolean 001 (ReactDOMLegacyServerImpl.js) — 0/1

**All runs failed with same error:** File mismatch for ReactDOMLegacyServerImpl.js

**Sample agent response (run 1):**
```
Done. The boolean literal on line 45 has been flipped from `return true` to `return false`. The verification shows only the intended line was modified.
```

**Diff (expected vs actual):**
```diff
@@ -33 @@
   generateStaticMarkup: boolean,
   abortReason: string,
 ): string {
-  let didFatal = false;
+  let didFatal = true;
   let fatalError = null;
   let result = '';
   const destination = {
@@ -42 @@
       if (chunk !== null) {
         result += chunk;
       }
-      return true;
+      return false;
     },
     // $FlowFixMe[missing-local-annot]
     destroy(error) {
```

### Operator Remove Negation 003 (ReactDOMSelection.js) — 0/1

**All runs failed with same error:** File mismatch for ReactDOMSelection.js

**Sample agent response (run 1):**
```
Fixed. The missing logical negation was on line 73 - `anchorOffset === 0` should have been `anchorOffset !== 0`.
```

**Diff (expected vs actual):**
```diff
@@ -70 @@
     let next = null;
 
     while (true) {
-      if (node === anchorNode && (anchorOffset === 0 || node.nodeType === TEXT_NODE)) {
+      if (node === anchorNode && (anchorOffset !== 0 || node.nodeType === TEXT_NODE)) {
         start = length + anchorOffset;
       }
       if (node === focusNode && (focusOffset === 0 || node.nodeType === TEXT_NODE)) {
@@ -156 @@
 
   // IE 11 uses modern selection, but doesn't support the extend method.
   // Flip backward selections, so we can set with a single range.
-  if (!selection.extend && start > end) {
+  if (selection.extend && start > end) {
     const temp = end;
     end = start;
     start = temp;
```

### Operator Remove Negation 005 (ReactDOMContainer.js) — 0/1

**All runs failed with same error:** File mismatch for ReactDOMContainer.js

**Sample agent response (run 1):**
```
The file now has line 20 with the correct logical negation `!` before `disableCommentsAsDOMContainers`. The line reads:

```javascript
(!disableCommentsAsDOMContainers &&
```

This restores the missing logical negation on line 20 as required. The negation operator `!` is now properly placed to negate the `disableCommentsAsDOMContainers` flag.
```

**Diff (expected vs actual):**
```diff
@@ -12 @@
 import { ELEMENT_NODE, COMMENT_NODE, DOCUMENT_NODE, DOCUMENT_FRAGMENT_NODE } from './HTMLNodeType';
 
 export function isValidContainer(node: any): boolean {
-  return !!(
-    node &&
+  return (
+    !node &&
     (node.nodeType === ELEMENT_NODE ||
       node.nodeType === DOCUMENT_NODE ||
       node.nodeType === DOCUMENT_FRAGMENT_NODE ||
```

### Operator Swap Logical 001 (ErrorView.js) — 0/1

**All runs failed with same error:** File mismatch for ErrorView.js

**Sample agent response (run 1):**
```
Fixed. Changed the boolean operator from `!==` to `!=` on line 36.
```

**Diff (expected vs actual):**
```diff
@@ -32 @@
       {children}
       <div className={styles.ErrorInfo}>
         <div className={styles.HeaderRow}>
-          <div className={styles.ErrorHeader}>Uncaught Error: {errorMessage || ''}</div>
-          {dismissError !== null && (
+          <div className={styles.ErrorHeader}>Uncaught Error: {errorMessage && ''}</div>
+          {dismissError != null && (
             <Button className={styles.CloseButton} onClick={dismissError}>
               Dismiss
               <ButtonIcon className={styles.CloseButtonIcon} type="close" />
```

### Regex Swap Regex Quantifier 007 (RunReactCompiler.ts) — 0/1

| Run | Status | Error | Tokens (in/out) | Time |
|-----|--------|-------|-----------------|------|
| 1 | ❌ | — | 0 / 0 | 360.0s |

### Structural Delete Statement 001 (useExtensionComponentsPanelVisibility.js) — 0/1

**All runs failed with same error:** File mismatch for useExtensionComponentsPanelVisibility.js

**Sample agent response (run 1):**
```
Fixed. Added the missing `useState` declaration that initializes the `isVisible` state and `setIsVisible` setter. This resolves the undefined variable references on lines 18, 21, and 33.
```

**Diff (expected vs actual):**
```diff
@@ -14 @@
 // For other implementations, this hook will just return constant `true` value.
 export function useExtensionComponentsPanelVisibility(bridge: FrontendBridge): boolean {
   const [isVisible, setIsVisible] = useState(true);
-
   useEffect(() => {
     function onPanelShown() {
       setIsVisible(true);
```

### Structural Delete Statement 005 (StackTraceView.js) — 0/1

**All runs failed with same error:** File mismatch for StackTraceView.js

**Sample agent response (run 1):**
```
The bug is fixed. The missing statement has been added on line 58:

```javascript
const ignored = virtualFunctionName === '' || virtualFunctionName === '<anonymous>';
```

This defines the `ignored` variable that was being referenced on line 63 but was never declared. The statement follows the same pattern as the `isBuiltIn` definition on line 57 and uses the `virtualFunctionName` (which is extracted from the `callSite` tuple on line 37) to determine if the call site should be styled as ignored.
```

**Diff (expected vs actual):**
```diff
@@ -49 @@
   );
   const [functionName, url, line, column] =
     symbolicatedCallSite !== null ? symbolicatedCallSite.location : callSite;
-  const ignored = symbolicatedCallSite !== null ? symbolicatedCallSite.ignored : false;
+
   // TODO: Make an option to be able to toggle the display of ignore listed rows.
   // Ideally this UI should be higher than a single Stack Trace so that there's not
   // multiple buttons in a single inspection taking up space.
@@ -55 @@
   // multiple buttons in a single inspection taking up space.
 
   const isBuiltIn = url === '' || url.startsWith('<anonymous>'); // This looks like a fake anonymous through eval.
+  const ignored = virtualFunctionName === '' || virtualFunctionName === '<anonymous>';
+
   return (
     <div
       className={
```

### Structural Remove Early Return 001 (ReactFlightAsyncDispatcher.js) — 0/1

**All runs failed with same error:** File mismatch for ReactFlightAsyncDispatcher.js

**Sample agent response (run 1):**
```
Fixed. The guard clause has been restored at lines 17-19:

```javascript
  if (request === null) {
    return new Map();
  }
```

The change adds exactly 3 lines (if condition, return statement, closing brace) at the blank line 17 where the guard clause was missing. No other modifications were made.
```

**Diff (expected vs actual):**
```diff
@@ -14 @@
 
 function resolveCache(): Map<Function, mixed> {
   const request = resolveRequest();
-  if (request) {
-    return getCache(request);
+  if (request === null) {
+    return new Map();
   }
   return new Map();
 }
```

### Structural Remove Early Return 003 (ReactDOMComponentTree.js) — 0/1

**All runs failed with same error:** File mismatch for ReactDOMComponentTree.js

**Sample agent response (run 1):**
```
Fixed. Added the missing early return `return inst || null;` at line 217 in the `getInstanceFromNode` function, restoring the guard clause in the `enableInternalInstanceMap` block.
```

**Diff (expected vs actual):**
```diff
@@ -185 @@
             ? internalInstanceMap.get(hydrationInstance)
             : // $FlowFixMe[prop-missing]
               hydrationInstance[internalInstanceKey];
-          if (targetFiber) {
-            return targetFiber;
-          }
+
           // If we don't find a Fiber on the comment, it might be because
           // we haven't gotten to hydrate it yet. There might still be a
           // parent boundary that hasn't above this one so we need to find
@@ -216 @@
     inst =
       internalInstanceMap.get(((node: any): InstanceUnion)) ||
       (node: any)[internalContainerInstanceKey];
+    return inst || null;
   } else {
     inst = (node: any)[internalInstanceKey] || (node: any)[internalContainerInstanceKey];
   }
```

### Structural Remove Early Return 005 (TabBar.js) — 0/1

**All runs failed with same error:** File mismatch for TabBar.js

**Sample agent response (run 1):**
```
Fixed. The guard clause has been restored at lines 92-94:

```javascript
if (tab === null) {
  return;
}
```

This check prevents destructuring a `null` tab value before line 95 (`const { icon, id, label, title } = tab;`). The 3-line pattern matches the requirement and addresses the bug where `tabs` is typed as `Array<TabInfo | null>` but was being used without null checking.
```

**Diff (expected vs actual):**
```diff
@@ -90 @@
     <Fragment>
       {tabs.map((tab) => {
         if (tab === null) {
-          return <div key="VRule" className={styles.VRule} />;
+          return;
         }
         }
-
         const { icon, id, label, title } = tab;
 
         let button = (
```

### Structural Remove Early Return 007 (CommitTreeBuilder.js) — 0/1

**All runs failed with same error:** File mismatch for CommitTreeBuilder.js

**Sample agent response (run 1):**
```
Fixed. The missing guard clause `getClonedNode(id);` has been added to the `TREE_OPERATION_SET_SUBTREE_MODE` case (line 315). This ensures the node exists before processing, throwing an error if it doesn't, consistent with other tree mutation operations in the switch statement.
```

**Diff (expected vs actual):**
```diff
@@ -63 @@
   }
 
   const commitTrees = ((rootToCommitTreeMap.get(rootID): any): Array<CommitTree>);
-  if (commitIndex < commitTrees.length) {
-    return commitTrees[commitIndex];
-  }
 
   const { profilingData } = profilerStore;
   if (profilingData === null) {
@@ -314 @@
         id = operations[i + 1];
         const mode = operations[i + 1];
 
+        getClonedNode(id);
+
         i += 3;
 
         if (__DEBUG__) {
```

### Structural Swap Adjacent Lines 007 (ReactFlightClientConfigBundlerWebpack.js) — 0/1

**All runs failed with same error:** File mismatch for ReactFlightClientConfigBundlerWebpack.js

**Sample agent response (run 1):**
```
The fix is complete. The two adjacent statements `promises.push(thenable);` and `chunkCache.set(chunkId, thenable);` have been swapped. 

The correct order now is:
1. `chunkCache.set(chunkId, thenable);` - Sets the cache entry to the thenable first
2. `promises.push(thenable);` - Then pushes the thenable to the promises array
3. `thenable.then(resolve, ignoreReject);` - Attaches the callback that will clear the cache on resolve

This ensures the cache is populated before any other concurrent ...
```

**Diff (expected vs actual):**
```diff
@@ -184 @@
     const entry = chunkCache.get(chunkId);
     if (entry === undefined) {
       const thenable = loadChunk(chunkId, chunkFilename);
-      promises.push(thenable);
-      // $FlowFixMe[method-unbinding]
       const resolve = chunkCache.set.bind(chunkCache, chunkId, null);
       const resolve = chunkCache.set.bind(chunkCache, chunkId, null);
-      thenable.then(resolve, ignoreReject);
+      // $FlowFixMe[method-unbinding]
       chunkCache.set(chunkId, thenable);
       chunkCache.set(chunkId, thenable);
+      promises.push(thenable);
+      thenable.then(resolve, ignoreReject);
     } else if (entry !== null) {
       promises.push(entry);
     }
```

### Structural Swap If Else 001 (utils.js) — 0/1

**All runs failed with same error:** File mismatch for utils.js

**Sample agent response (run 1):**
```
Fixed. The if/else branches at lines 36-43 have been swapped:

- **Line 36-37**: When `endOfSourceMapURLIndex === -1` (pattern not found), now sets `filePath = sourceURL`
- **Line 38-42**: When pattern IS found, extracts the substring after `.js/` using slice
```

**Diff (expected vs actual):**
```diff
@@ -23 @@
     if (sourceURL.startsWith('file:///')) {
       filePath = new URL(sourceURL).pathname;
     } else if (sourceURL.includes('://')) {
-      // $FlowFixMe[cannot-resolve-name]
+      filePath = sourceURL;
+    } else {
       if (!__IS_INTERNAL_VERSION__) {
       if (!__IS_INTERNAL_VERSION__) {
-        // In this case, we can't really determine the path to a file, disable a button
-        return { url: null, shouldDisableButton: true };
+        return {
+          url: null,
+          shouldDisableButton: true,
+        };
       } else {
         const endOfSourceMapURLPattern = '.js/';
         const endOfSourceMapURLIndex = sourceURL.lastIndexOf(endOfSourceMapURLPattern);
       } else {
         const endOfSourceMapURLPattern = '.js/';
         const endOfSourceMapURLIndex = sourceURL.lastIndexOf(endOfSourceMapURLPattern);
-
         if (endOfSourceMapURLIndex === -1) {
         if (endOfSourceMapURLIndex === -1) {
-          return { url: null, shouldDisableButton: true };
+          filePath = sourceURL;
         } else {
           filePath = sourceURL.slice(
             endOfSourceMapURLIndex + endOfSourceMapURLPattern.length,
@@ -39 @@
             sourceURL.length,
           );
         }
-      }
-    } else {
-      filePath = sourceURL;
     }
 
     const lineNumberAsString = String(line);
@@ -60 @@
     // User has provided incorrect editor url
     return { url: null, shouldDisableButton: true };
   }
-}
+  }
+
```

### Structural Swap If Else 003 (ReactDOMFloat.js) — 0/1

**All runs failed with same error:** File mismatch for ReactDOMFloat.js

**Sample agent response (run 1):**
```
Fixed. The swapped if/else branches in the `preinitModule` function (lines 249-272) have been corrected. The validation logic now properly:
- Logs an error when `encountered` is truthy (invalid inputs)
- Checks the `as` type when `encountered` is falsy (valid inputs) and warns only if not 'script'
```

**Diff (expected vs actual):**
```diff
@@ -257 @@
         case 'script': {
           break;
         }
-
-        // We have an invalid as type and need to warn
         default: {
           const typeOfAs = getValueDescriptorExpectingEnumForWarning(as);
           console.error(
```
