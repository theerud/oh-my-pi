# Edit Benchmark Report

## Configuration

| Setting | Value |
|---------|-------|
| Date | 2026-02-20T09:52:27.454Z |
| Model | kimi-code/kimi-code/kimi-k2.5 |
| Thinking Level | low |
| Runs per task | 1 |
| Edit Variant | hashline |
| Edit Fuzzy | auto |
| Edit Fuzzy Threshold | auto |
| Guided Mode | no |
| Max Attempts | 1 |
| No-op Retry Limit | 2 |
| Mutation Scope Window | 20 |
| Require Edit Tool | no |
| Require Read Tool | no |
| No-Edit Baseline | no |

## Summary

| Metric | Value |
|--------|-------|
| Total Tasks | 80 |
| Total Runs | 80 |
| Successful Runs | 61 |
| **Task Success Rate** | **76.3% (61/80)** |
| Verified Rate | 76.3% (61/80) |
| Edit Tool Usage Rate | 95.0% (76/80) |
| **Edit Success Rate** | **91.2%** |
| Timeout Runs | 0 |
| Mutation Intent Match Rate | 78.9% |
| Patch Failure Rate | 8.8% (8/91) |
| Tasks All Passing | 61 |
| Tasks Flaky/Failing | 19 |

### Tool Calls

| Tool | Total | Avg/Run |
|------|-------|---------|
| Read | 171 | 2.1 |
| Edit | 91 | 1.1 |
| Write | 0 | 0.0 |
| **Tool Input Chars** | 22,935 | 287 |

### Tokens & Time

| Metric | Total | Avg/Run |
|--------|-------|---------|
| Input Tokens | 3,348,527 | 41,857 |
| Output Tokens | 94,894 | 1,186 |
| Total Tokens | 6,315,741 | 78,947 |
| Duration | 1512.3s | 18.9s |
| **Avg Indent Score** | — | **0.07** |

## Task Results

| Task | File | Success | Edit Hit | R/E/W | Tokens (In/Out) | Time | Indent |
|------|------|---------|----------|-------|-----------------|------|--------|
| Access Remove Optional Chain 001 | fallbackEvalContext.js | 1/1 ✅ | 100.0% | 2/1/0 | 24,707/521 | 9.3s | 0.00 |
| Access Remove Optional Chain 003 | hookNamesCache.js | 1/1 ✅ | 100.0% | 2/1/0 | 30,777/647 | 11.1s | 0.00 |
| Access Remove Optional Chain 005 | registerDevToolsEventLogger.js | 1/1 ✅ | 100.0% | 2/1/0 | 26,939/969 | 14.1s | 0.00 |
| Access Remove Optional Chain 007 | index.js | 0/1 ❌ | 50.0% | 3/2/0 | 77,620/1,300 | 22.1s | 0.00 |
| Call Swap Call Args 001 | formatConsoleArguments.js | 1/1 ✅ | 100.0% | 2/1/0 | 25,481/497 | 9.2s | 0.00 |
| Call Swap Call Args 003 | ReactFlightDOMServerBrowser.js | 1/1 ✅ | 100.0% | 3/1/0 | 40,955/730 | 13.4s | 0.00 |
| Call Swap Call Args 005 | ReactNoopPersistent.js | 1/1 ✅ | 100.0% | 2/1/0 | 26,005/967 | 13.9s | 0.00 |
| Call Swap Call Args 007 | parseSourceAndMetadata.js | 1/1 ✅ | 100.0% | 2/1/0 | 37,923/756 | 12.6s | 0.00 |
| Duplicate Duplicate Line Flip 001 | isCustomElement.js | 1/1 ✅ | 100.0% | 2/1/0 | 25,233/846 | 12.4s | 0.00 |
| Duplicate Duplicate Line Flip 003 | ReactFiberDevToolsHook.js | 0/1 ❌ | 100.0% | 3/1/0 | 58,371/3,873 | 44.9s | 0.00 |
| Duplicate Duplicate Line Flip 005 | shallowEqual.js | 1/1 ✅ | 100.0% | 2/1/0 | 25,402/571 | 10.9s | 0.00 |
| Duplicate Duplicate Line Flip 007 | ReactDOMEventReplaying.js | 1/1 ✅ | 100.0% | 3/1/0 | 62,868/1,490 | 22.2s | 0.00 |
| Identifier Identifier Multi Edit 001 | Button.js | 1/1 ✅ | 100.0% | 2/1/0 | 24,883/454 | 8.8s | 0.00 |
| Identifier Identifier Multi Edit 003 | ReactFlightDOMClientBrowser.js | 1/1 ✅ | 100.0% | 2/1/0 | 31,827/739 | 12.1s | 0.00 |
| Identifier Identifier Multi Edit 005 | githubAPI.js | 0/1 ❌ | 100.0% | 0/0/0 | 0/0 | 0ms | 0.00 |
| Identifier Identifier Multi Edit 007 | ReactFiberComponentStack.js | 1/1 ✅ | 100.0% | 2/1/0 | 30,734/510 | 10.1s | 0.00 |
| Import Swap Named Imports 001 | ListApp.js | 1/1 ✅ | 100.0% | 2/1/0 | 25,532/850 | 12.7s | 0.00 |
| Import Swap Named Imports 003 | index.js | 0/1 ❌ | 100.0% | 0/0/0 | 0/0 | 0ms | 0.00 |
| Import Swap Named Imports 005 | SuspenseScrubber.js | 1/1 ✅ | 100.0% | 2/1/0 | 27,858/644 | 11.0s | 0.00 |
| Import Swap Named Imports 007 | InspectedElement.js | 1/1 ✅ | 83.3% | 5/6/0 | 707,275/4,079 | 214.7s | 0.00 |
| Literal Flip Boolean 001 | ReactDOMLegacyServerImpl.js | 1/1 ✅ | 100.0% | 2/1/0 | 27,903/938 | 14.4s | 0.00 |
| Literal Flip Boolean 003 | ReactProfilerTimer.js | 1/1 ✅ | 100.0% | 3/1/0 | 63,547/1,138 | 18.2s | 0.00 |
| Literal Flip Boolean 005 | OpenInEditorButton.js | 1/1 ✅ | 100.0% | 2/1/0 | 25,641/563 | 9.9s | 0.00 |
| Literal Flip Boolean 007 | Element.js | 1/1 ✅ | 100.0% | 2/1/0 | 32,576/702 | 11.7s | 0.00 |
| Literal Off By One 001 | ReactOwnerStackFrames.js | 1/1 ✅ | 100.0% | 2/1/0 | 25,126/621 | 10.6s | 0.00 |
| Literal Off By One 003 | ReactFlightClientConfigBundlerTurbopack.js | 1/1 ✅ | 100.0% | 2/1/0 | 23,137/467 | 11.6s | 0.00 |
| Literal Off By One 005 | ContextMenu.js | 1/1 ✅ | 100.0% | 2/1/0 | 26,961/506 | 9.7s | 0.00 |
| Literal Off By One 007 | index.js | 1/1 ✅ | 100.0% | 2/1/0 | 46,619/580 | 11.6s | 0.00 |
| Operator Remove Negation 001 | prepareInjection.js | 1/1 ✅ | 100.0% | 2/1/0 | 24,753/526 | 10.0s | 0.00 |
| Operator Remove Negation 003 | ReactDOMSelection.js | 1/1 ✅ | 50.0% | 2/2/0 | 49,936/4,512 | 52.4s | 0.00 |
| Operator Remove Negation 005 | ReactDOMContainer.js | 0/1 ❌ | 100.0% | 2/1/0 | 24,934/847 | 13.2s | 0.00 |
| Operator Remove Negation 007 | SelectEventPlugin.js | 1/1 ✅ | 100.0% | 2/1/0 | 29,887/471 | 10.5s | 0.00 |
| Operator Swap Arithmetic 001 | formatConsoleArguments.js | 1/1 ✅ | 100.0% | 2/1/0 | 25,380/518 | 9.4s | 0.00 |
| Operator Swap Arithmetic 003 | ReactFlightDOMServerBrowser.js | 1/1 ✅ | 100.0% | 2/1/0 | 32,138/1,009 | 14.5s | 0.00 |
| Operator Swap Arithmetic 005 | ReactFiberConfigWithNoResources.js | 1/1 ✅ | 100.0% | 2/1/0 | 24,790/500 | 9.8s | 0.00 |
| Operator Swap Arithmetic 007 | useCanvasInteraction.js | 1/1 ✅ | 100.0% | 2/1/0 | 31,347/829 | 13.2s | 0.00 |
| Operator Swap Comparison 001 | useSmartTooltip.js | 1/1 ✅ | 100.0% | 2/1/0 | 27,761/852 | 12.6s | 0.00 |
| Operator Swap Comparison 003 | ReactFlightDOMServerBrowser.js | 1/1 ✅ | 100.0% | 2/1/0 | 32,978/995 | 15.0s | 0.00 |
| Operator Swap Comparison 005 | Rectangle.js | 1/1 ✅ | 100.0% | 2/1/0 | 27,360/562 | 17.0s | 0.00 |
| Operator Swap Comparison 007 | ReactFiberTreeReflection.js | 1/1 ✅ | 100.0% | 2/1/0 | 44,697/890 | 15.7s | 0.00 |
| Operator Swap Equality 001 | ReactNoopFlightClient.js | 1/1 ✅ | 100.0% | 2/1/0 | 27,913/877 | 13.2s | 0.00 |
| Operator Swap Equality 003 | astUtils.js | 1/1 ✅ | 100.0% | 2/1/0 | 46,735/844 | 14.3s | 0.00 |
| Operator Swap Equality 005 | ReactDOMContainer.js | 1/1 ✅ | 100.0% | 2/1/0 | 23,995/351 | 7.8s | 0.00 |
| Operator Swap Equality 007 | ReactFlightDOMServerBrowser.js | 0/1 ❌ | 100.0% | 0/0/0 | 0/0 | 0ms | 0.00 |
| Operator Swap Increment Decrement 001 | index.js | 1/1 ✅ | 100.0% | 2/1/0 | 24,543/459 | 8.9s | 0.00 |
| Operator Swap Increment Decrement 003 | ReactFlightClientConfigBundlerESM.js | 1/1 ✅ | 100.0% | 4/2/0 | 58,180/956 | 18.2s | 0.00 |
| Operator Swap Increment Decrement 005 | ReactFiberViewTransitionComponent.js | 1/1 ✅ | 100.0% | 2/1/0 | 26,755/432 | 8.9s | 0.00 |
| Operator Swap Increment Decrement 007 | ReactFiberConcurrentUpdates.js | 1/1 ✅ | 100.0% | 2/1/0 | 34,789/951 | 15.0s | 0.00 |
| Operator Swap Logical 001 | ErrorView.js | 0/1 ❌ | 100.0% | 2/1/0 | 43,488/10,013 | 104.9s | 0.00 |
| Operator Swap Logical 003 | DevTools.js | 1/1 ✅ | 100.0% | 2/1/0 | 37,123/991 | 14.6s | 0.00 |
| Operator Swap Logical 005 | UseEffectEvent.js | 1/1 ✅ | 100.0% | 2/1/0 | 25,185/883 | 12.4s | 0.00 |
| Operator Swap Logical 007 | getHookNameForLocation.js | 0/1 ❌ | 100.0% | 2/1/0 | 34,696/1,624 | 19.9s | 0.00 |
| Operator Swap Nullish 001 | ElementBadges.js | 1/1 ✅ | 100.0% | 2/1/0 | 25,705/694 | 11.6s | 0.00 |
| Operator Swap Nullish 003 | ReactComponentStackFrame.js | 1/1 ✅ | 100.0% | 2/1/0 | 34,867/670 | 11.9s | 0.00 |
| Operator Swap Nullish 005 | ReactLogo.js | 1/1 ✅ | 100.0% | 2/1/0 | 25,432/787 | 11.7s | 0.00 |
| Operator Swap Nullish 007 | SuspenseBreadcrumbs.js | 1/1 ✅ | 100.0% | 4/2/0 | 65,645/1,182 | 21.1s | 0.00 |
| Regex Swap Regex Quantifier 001 | SimpleValues.js | 1/1 ✅ | 100.0% | 2/1/0 | 24,535/384 | 8.4s | 0.00 |
| Regex Swap Regex Quantifier 003 | utils.js | 1/1 ✅ | 100.0% | 3/1/0 | 52,860/2,488 | 31.0s | 0.00 |
| Regex Swap Regex Quantifier 005 | formatWithStyles.js | 1/1 ✅ | 100.0% | 2/1/0 | 26,162/588 | 10.2s | 0.00 |
| Regex Swap Regex Quantifier 007 | RunReactCompiler.ts | 0/1 ❌ | 100.0% | 2/1/0 | 55,266/10,863 | 110.4s | 0.00 |
| Structural Delete Statement 001 | useExtensionComponentsPanelVisibility.js | 0/1 ❌ | 100.0% | 2/1/0 | 24,927/742 | 11.4s | 0.00 |
| Structural Delete Statement 003 | useCanvasInteraction.js | 1/1 ✅ | 50.0% | 2/2/0 | 40,511/1,025 | 15.8s | 0.00 |
| Structural Delete Statement 005 | StackTraceView.js | 0/1 ❌ | 100.0% | 2/1/0 | 27,627/898 | 13.1s | 0.00 |
| Structural Delete Statement 007 | ReactDOMFizzStaticBrowser.js | 1/1 ✅ | 50.0% | 4/2/0 | 59,000/1,587 | 23.3s | 5.56 |
| Structural Remove Early Return 001 | ReactFlightAsyncDispatcher.js | 0/1 ❌ | 50.0% | 2/2/0 | 33,318/2,209 | 25.5s | 0.00 |
| Structural Remove Early Return 003 | ReactDOMComponentTree.js | 0/1 ❌ | 50.0% | 2/2/0 | 49,574/1,126 | 18.0s | 0.00 |
| Structural Remove Early Return 005 | TabBar.js | 0/1 ❌ | 100.0% | 2/1/0 | 27,901/733 | 11.2s | 0.00 |
| Structural Remove Early Return 007 | CommitTreeBuilder.js | 0/1 ❌ | 100.0% | 3/1/0 | 53,052/1,356 | 19.7s | 0.00 |
| Structural Swap Adjacent Lines 001 | reactPolling.js | 1/1 ✅ | 100.0% | 2/1/0 | 26,671/606 | 10.2s | 0.00 |
| Structural Swap Adjacent Lines 003 | OwnersStack.js | 1/1 ✅ | 100.0% | 2/1/0 | 33,768/782 | 12.5s | 0.00 |
| Structural Swap Adjacent Lines 005 | ReactOwnerStackFrames.js | 1/1 ✅ | 66.7% | 4/3/0 | 59,240/1,781 | 27.1s | 0.00 |
| Structural Swap Adjacent Lines 007 | ReactFlightClientConfigBundlerWebpack.js | 0/1 ❌ | 100.0% | 3/1/0 | 46,011/1,734 | 22.1s | 0.00 |
| Structural Swap If Else 001 | utils.js | 0/1 ❌ | 100.0% | 2/1/0 | 27,208/1,095 | 15.3s | 0.00 |
| Structural Swap If Else 003 | ReactDOMFloat.js | 0/1 ❌ | 100.0% | 2/1/0 | 38,377/1,209 | 17.4s | 0.00 |
| Structural Swap If Else 005 | ReactClientConsoleConfigPlain.js | 1/1 ✅ | 100.0% | 2/1/0 | 26,334/1,053 | 14.0s | 0.00 |
| Structural Swap If Else 007 | index.js | 1/1 ✅ | 100.0% | 2/1/0 | 33,375/1,043 | 15.7s | 0.00 |
| Unicode Unicode Hyphen 001 | formatProdErrorMessage.js | 1/1 ✅ | 100.0% | 2/1/0 | 24,137/365 | 7.9s | 0.00 |
| Unicode Unicode Hyphen 003 | SourceMapConsumer.js | 1/1 ✅ | 100.0% | 2/1/0 | 30,749/581 | 10.9s | 0.00 |
| Unicode Unicode Hyphen 005 | babel.config.js | 0/1 ❌ | 100.0% | 0/0/0 | 0/0 | 0ms | 0.00 |
| Unicode Unicode Hyphen 007 | ReactInternalTestUtils.js | 1/1 ✅ | 100.0% | 2/1/0 | 35,012/463 | 10.5s | 0.00 |

## Category Summary

| Category | Runs | Verified | Edit Used | Success | Min/Avg/Max Difficulty |
|----------|------|----------|-----------|---------|------------------------|
| access | 4 | 75.0% (3/4) | 100.0% (4/4) | 75.0% (3/4) | 3 / 7.5 / 10 |
| call | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) | 2 / 7.8 / 14 |
| duplicate | 4 | 75.0% (3/4) | 100.0% (4/4) | 75.0% (3/4) | 8 / 11.3 / 17 |
| identifier | 3 | 100.0% (3/3) | 100.0% (3/3) | 100.0% (3/3) | 4 / 7.0 / 9 |
| import | 3 | 100.0% (3/3) | 100.0% (3/3) | 100.0% (3/3) | 2 / 3.3 / 5 |
| literal | 8 | 100.0% (8/8) | 100.0% (8/8) | 100.0% (8/8) | 3 / 6.9 / 10 |
| operator | 27 | 88.9% (24/27) | 100.0% (27/27) | 88.9% (24/27) | 3 / 6.6 / 10 |
| regex | 4 | 75.0% (3/4) | 100.0% (4/4) | 75.0% (3/4) | 2 / 5.8 / 10 |
| structural | 16 | 43.8% (7/16) | 100.0% (16/16) | 43.8% (7/16) | 4 / 8.3 / 13 |
| unicode | 3 | 100.0% (3/3) | 100.0% (3/3) | 100.0% (3/3) | 4 / 5.7 / 8 |
| unknown | 4 | 0.0% (0/4) | 0.0% (0/4) | 0.0% (0/4) | — |

## Mutation Summary

| Mutation | Category | Runs | Verified | Edit Used | Success |
|----------|----------|------|----------|-----------|---------|
| delete-statement | structural | 4 | 50.0% (2/4) | 100.0% (4/4) | 50.0% (2/4) |
| duplicate-line-flip | duplicate | 4 | 75.0% (3/4) | 100.0% (4/4) | 75.0% (3/4) |
| flip-boolean | literal | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) |
| identifier-multi-edit | identifier | 3 | 100.0% (3/3) | 100.0% (3/3) | 100.0% (3/3) |
| off-by-one | literal | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) |
| remove-early-return | structural | 4 | 0.0% (0/4) | 100.0% (4/4) | 0.0% (0/4) |
| remove-negation | operator | 4 | 75.0% (3/4) | 100.0% (4/4) | 75.0% (3/4) |
| remove-optional-chain | access | 4 | 75.0% (3/4) | 100.0% (4/4) | 75.0% (3/4) |
| swap-adjacent-lines | structural | 4 | 75.0% (3/4) | 100.0% (4/4) | 75.0% (3/4) |
| swap-arithmetic | operator | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) |
| swap-call-args | call | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) |
| swap-comparison | operator | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) |
| swap-equality | operator | 3 | 100.0% (3/3) | 100.0% (3/3) | 100.0% (3/3) |
| swap-if-else | structural | 4 | 50.0% (2/4) | 100.0% (4/4) | 50.0% (2/4) |
| swap-increment-decrement | operator | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) |
| swap-logical | operator | 4 | 50.0% (2/4) | 100.0% (4/4) | 50.0% (2/4) |
| swap-named-imports | import | 3 | 100.0% (3/3) | 100.0% (3/3) | 100.0% (3/3) |
| swap-nullish | operator | 4 | 100.0% (4/4) | 100.0% (4/4) | 100.0% (4/4) |
| swap-regex-quantifier | regex | 4 | 75.0% (3/4) | 100.0% (4/4) | 75.0% (3/4) |
| unicode-hyphen | unicode | 3 | 100.0% (3/3) | 100.0% (3/3) | 100.0% (3/3) |
| unknown | unknown | 4 | 0.0% (0/4) | 0.0% (0/4) | 0.0% (0/4) |

## Difficulty Summary

| Difficulty Score | Runs | Verified | Edit Used | Success |
|------------------|------|----------|-----------|---------|
| 0-2 | 3 | 100.0% (3/3) | 100.0% (3/3) | 100.0% (3/3) |
| 3-5 | 23 | 82.6% (19/23) | 100.0% (23/23) | 82.6% (19/23) |
| 6-8 | 26 | 80.8% (21/26) | 100.0% (26/26) | 80.8% (21/26) |
| 9+ | 24 | 75.0% (18/24) | 100.0% (24/24) | 75.0% (18/24) |
| unknown | 4 | 0.0% (0/4) | 0.0% (0/4) | 0.0% (0/4) |

## Edit Tool Errors

Failures where the edit tool returned an error or failed to apply the patch.

### Access Remove Optional Chain 007 (index.js)

#### Run 1

##### Attempt 1

- Path: index.js
- Operation: —

**Tool error**

```
1 line has changed since last read. Use the updated LINE#ID references shown below (>>> marks changed lines).

    420#QT:  clearReactPollingInstance();
    421#ZR:
>>> 422#NS:  if (store !== null) {
    423#BN:    // Store profiling data, so it can be used later
    424#JQ:    profilingData = store.profilerStore.profilingData;
```

**Patch args**

```diff
{
  "path": "index.js",
  "edits": [
    {
      "op": "set",
      "tag": "422#JQ",
      "content": [
        "    profilingData = store.profilerStore?.profilingData;"
      ]
    }
  ]
}
```

### Import Swap Named Imports 007 (InspectedElement.js)

#### Run 1

##### Attempt 1

- Path: InspectedElement.js
- Operation: —

**Tool error**

```
Validation failed for tool "edit":
  - edits/0: must have required property 'tag'
  - edits/0: must NOT have additional properties
  - edits/0: must NOT have additional properties
  - edits/0: must have required property 'first'
  - edits/0: must have required property 'last'
  - edits/0: must NOT have additional properties
  - edits/0: must NOT have additional properties
  - edits/0/op: must be equal to constant
  - edits/0: must NOT have additional properties
  - edits/0/op: must be equal to constant
  - edits/0: must NOT have additional properties
  - edits/0/op: must be equal to constant
  - edits/0/op: must be equal to constant
  - edits/0: must match a schema in anyOf

Received arguments:
{
  "path": "InspectedElement.js",
  "edits": [
    {
      "op": "set",
      "content": "import { useSyncExternalStore, useContext, useCallback } from 'react';",
      "after": "12#YM",
      "before": "14#SZ"
    }
  ]
}
```

**Patch args**

```diff
{
  "path": "InspectedElement.js",
  "edits": [
    {
      "op": "set",
      "content": "import { useSyncExternalStore, useContext, useCallback } from 'react';",
      "after": "12#YM",
      "before": "14#SZ"
    }
  ]
}
```

### Operator Remove Negation 003 (ReactDOMSelection.js)

#### Run 1

##### Attempt 1

- Path: ReactDOMSelection.js
- Operation: —

**Tool error**

```
Invalid line reference "VT". Expected format "LINE#ID" (e.g. "5#aa").
```

**Patch args**

```diff
{
  "path": "ReactDOMSelection.js",
  "edits": [
    {
      "op": "set",
      "tag": "VT",
      "content": [
        "  if (!selection.extend && start > end) {"
      ]
    }
  ]
}
```

### Structural Delete Statement 003 (useCanvasInteraction.js)

#### Run 1

##### Attempt 1

- Path: useCanvasInteraction.js
- Operation: —

**Tool error**

```
No changes made to useCanvasInteraction.js. The edits produced identical content.
Edit 0: replacement for 163#MK is identical to current content:
  163#MK|       didMouseMoveWhileDownRef.current = false;
Your content must differ from what the file already contains. Re-read the file to see the current state.
The file differs from the original fixture at these lines:
164#PQ|+      isMouseDownRef.current = true;
```

**Patch args**

```diff
{
  "path": "useCanvasInteraction.js",
  "edits": [
    {
      "op": "set",
      "tag": "163#MK",
      "content": [
        "      didMouseMoveWhileDownRef.current = false;",
        "      isMouseDownRef.current = true;"
      ]
    }
  ]
}
```

### Structural Delete Statement 007 (ReactDOMFizzStaticBrowser.js)

#### Run 1

##### Attempt 1

- Path: ReactDOMFizzStaticBrowser.js
- Operation: —

**Tool error**

```
1 line has changed since last read. Use the updated LINE#ID references shown below (>>> marks changed lines).

    69#VM:    const onFatalError = reject;
    70#ZR:
>>> 71#NV:    function onAllReady() {
    72#WH:      const result: StaticResult = {
    73#YQ:        postponed: getPostponedState(request),
```

**Patch args**

```diff
{
  "path": "ReactDOMFizzStaticBrowser.js",
  "edits": [
    {
      "op": "insert",
      "content": [
        "    const stream = new ReadableStream(",
        "      {",
        "        type: 'bytes',",
        "        pull: (controller): ?Promise<void> => {",
        "          startFlowing(request, controller);",
        "        },",
        "        cancel: (reason): ?Promise<void> => {",
        "          stopFlowing(request);",
        "          abort(request, reason);",
        "        },",
        "      },",
        "      // $FlowFixMe[prop-missing] size() methods are not allowed on byte streams.",
        "      { highWaterMark: 0 },",
        "    );",
        "",
        "],",
        "   before: ",
        "\n    after: ",
        "\n    ",
        "72#WH:      const result: StaticResult = {",
        "73#YQ:        postponed: getPostponedState(request),",
        "74#TK:        prelude: stream,"
      ],
      "after": "71#TK:",
      "before": "72#WH"
    }
  ]
}
```

### Structural Remove Early Return 001 (ReactFlightAsyncDispatcher.js)

#### Run 1

##### Attempt 1

- Path: ReactFlightAsyncDispatcher.js
- Operation: —

**Tool error**

```
Validation failed for tool "edit":
  - edits/0/op: must be equal to constant
  - edits/0: must have required property 'first'
  - edits/0: must have required property 'last'
  - edits/0: must NOT have additional properties
  - edits/0/op: must be equal to constant
  - edits/0: must NOT have additional properties
  - edits/0/op: must be equal to constant
  - edits/0: must NOT have additional properties
  - edits/0/op: must be equal to constant
  - edits/0: must NOT have additional properties
  - edits/0: must match a schema in anyOf

Received arguments:
{
  "path": "ReactFlightAsyncDispatcher.js",
  "edits": [
    {
      "op": "insert",
      "tag": "17#ZR",
      "content": [
        "  if (!request) {",
        "    return new Map();",
        "  }"
      ]
    }
  ]
}
```

**Patch args**

```diff
{
  "path": "ReactFlightAsyncDispatcher.js",
  "edits": [
    {
      "op": "insert",
      "tag": "17#ZR",
      "content": [
        "  if (!request) {",
        "    return new Map();",
        "  }"
      ]
    }
  ]
}
```

### Structural Remove Early Return 003 (ReactDOMComponentTree.js)

#### Run 1

##### Attempt 1

- Path: ReactDOMComponentTree.js
- Operation: —

**Tool error**

```
Validation failed for tool "edit":
  - edits/0/op: must be equal to constant
  - edits/0: must have required property 'first'
  - edits/0: must have required property 'last'
  - edits/0: must NOT have additional properties
  - edits/0/op: must be equal to constant
  - edits/0: must NOT have additional properties
  - edits/0/op: must be equal to constant
  - edits/0: must NOT have additional properties
  - edits/0/op: must be equal to constant
  - edits/0: must NOT have additional properties
  - edits/0: must match a schema in anyOf

Received arguments:
{
  "path": "ReactDOMComponentTree.js",
  "edits": [
    {
      "op": "insert",
      "tag": "187#XW",
      "content": [
        "          if (!targetFiber) {",
        "            continue;",
        "          }"
      ]
    }
  ]
}
```

**Patch args**

```diff
{
  "path": "ReactDOMComponentTree.js",
  "edits": [
    {
      "op": "insert",
      "tag": "187#XW",
      "content": [
        "          if (!targetFiber) {",
        "            continue;",
        "          }"
      ]
    }
  ]
}
```

### Structural Swap Adjacent Lines 005 (ReactOwnerStackFrames.js)

#### Run 1

##### Attempt 1

- Path: ReactOwnerStackFrames.js
- Operation: —

**Tool error**

```
Invalid line reference "BP". Expected format "LINE#ID" (e.g. "5#aa").
```

**Patch args**

```diff
{
  "path": "ReactOwnerStackFrames.js",
  "edits": [
    {
      "op": "set",
      "tag": "BP",
      "content": [
        "  Error.prepareStackTrace = DefaultPrepareStackTrace;"
      ]
    },
    {
      "op": "set",
      "tag": "QY",
      "content": [
        "  let stack = error.stack;"
      ]
    }
  ]
}
```

## Failed Tasks (0% passing)

### Access Remove Optional Chain 007 (index.js) — 0/1

**All runs failed with same error:** File mismatch for index.js

**Sample agent response (run 1):**
```
Fixed. Added optional chaining (`?.`) to line 424, changing:
```javascript
profilingData = store.profilerStore.profilingData;
```
to:
```javascript
profilingData = store.profilerStore?.profilingData;
```

This guards against `store.profilerStore` being undefined/null while preserving the profiling data during in-tab navigation cleanup.
```

**Diff (expected vs actual):**
```diff
@@ -64 @@
     },
 
     send(event: string, payload: any, transferable?: Array<any>) {
-      port?.postMessage({ event, payload }, transferable);
+      port.postMessage({ event, payload }, transferable);
     },
   });
 
@@ -421 @@
 
   if (store !== null) {
     // Store profiling data, so it can be used later
-    profilingData = store.profilerStore.profilingData;
+    profilingData = store.profilerStore?.profilingData;
   }
 
   // If panels were already created, and we have already mounted React root to display
```

### Duplicate Duplicate Line Flip 003 (ReactFiberDevToolsHook.js) — 0/1

**All runs failed with same error:** File mismatch for ReactFiberDevToolsHook.js

**Sample agent response (run 1):**
```
Fix confirmed. Line 124 now correctly uses `IdleSchedulerPriority` instead of the duplicated `NormalSchedulerPriority`. The default case now properly aligns with the `IdleEventPriority` case above it, fixing the subtle literal change bug.
```

**Diff (expected vs actual):**
```diff
@@ -46 @@
 export function injectInternals(internals: Object): boolean {
   if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === 'undefined') {
     // No DevTools
-    return false;
+    return true;
   }
   const hook = __REACT_DEVTOOLS_GLOBAL_HOOK__;
   if (hook.isDisabled) {
@@ -121 @@
             schedulerPriority = IdleSchedulerPriority;
             break;
           default:
-            schedulerPriority = NormalSchedulerPriority;
+            schedulerPriority = IdleSchedulerPriority;
             break;
         }
         injectedHook.onCommitFiberRoot(rendererID, root, schedulerPriority, didError);
```

### Identifier Identifier Multi Edit 005 (githubAPI.js) — 0/1

**All runs failed with same error:** Timeout waiting for response to set_thinking_level. Stderr: 365 \| 			let response: Response \| undefined;              ^ error: "response" has already been declared     at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:365:8  360 \| 			let response: Response \| undefined;              ^ note: "response" was originally declared here    at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:360:8  366 \| 			let lastError: Error \| undefined;              ^ error: "lastError" has already been declared     at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:366:8  361 \| 			let lastError: Error \| undefined;              ^ note: "lastError" was originally declared here    at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:361:8  367 \| 			let requestUrl: string \| undefined;              ^ error: "requestUrl" has already been declared     at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:367:8  362 \| 			let requestUrl: string \| undefined;              ^ note: "requestUrl" was originally declared here    at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:362:8  Bun v1.3.10-canary.52+5b0db0191 (macOS arm64) 

### Import Swap Named Imports 003 (index.js) — 0/1

**All runs failed with same error:** Timeout waiting for response to set_thinking_level. Stderr: 102 \| const MAX_EMPTY_STREAM_RETRIES = 2;             ^ error: "MAX_EMPTY_STREAM_RETRIES" has already been declared     at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:102:7  100 \| const MAX_EMPTY_STREAM_RETRIES = 2;             ^ note: "MAX_EMPTY_STREAM_RETRIES" was originally declared here    at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:100:7  103 \| const EMPTY_STREAM_BASE_DELAY_MS = 500;             ^ error: "EMPTY_STREAM_BASE_DELAY_MS" has already been declared     at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:103:7  101 \| const EMPTY_STREAM_BASE_DELAY_MS = 500;             ^ note: "EMPTY_STREAM_BASE_DELAY_MS" was originally declared here    at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:101:7  367 \| 			let response: Response \| undefined;              ^ error: "response" has already been declared     at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:367:8  362 \| 			let response: Response \| undefined;              ^ note: "response" was originally declared here    at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:362:8  368 \| 			let lastError: Error \| undefined;              ^ error: "lastError" has already been declared     at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:368:8  363 \| 			let lastError: Error \| undefined;              ^ note: "lastError" was originally declared here    at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:363:8  369 \| 			let requestUrl: string \| undefined;              ^ error: "requestUrl" has already been declared     at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:369:8  364 \| 			let requestUrl: string \| undefined;              ^ note: "requestUrl" was originally declared here    at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:364:8  Bun v1.3.10-canary.52+5b0db0191 (macOS arm64) 

### Operator Remove Negation 005 (ReactDOMContainer.js) — 0/1

**All runs failed with same error:** File mismatch for ReactDOMContainer.js

**Sample agent response (run 1):**
```
Fix applied. Line 16 now correctly reads `!!node &&` instead of `!node &&`. The double negation coerces `node` to a boolean before the logical AND chain continues.
```

**Diff (expected vs actual):**
```diff
@@ -12 @@
 import { ELEMENT_NODE, COMMENT_NODE, DOCUMENT_NODE, DOCUMENT_FRAGMENT_NODE } from './HTMLNodeType';
 
 export function isValidContainer(node: any): boolean {
-  return !!(
-    node &&
+  return (
+    !!node &&
     (node.nodeType === ELEMENT_NODE ||
       node.nodeType === DOCUMENT_NODE ||
       node.nodeType === DOCUMENT_FRAGMENT_NODE ||
```

### Operator Swap Equality 007 (ReactFlightDOMServerBrowser.js) — 0/1

**All runs failed with same error:** Timeout waiting for response to set_thinking_level. Stderr: 102 \| const MAX_EMPTY_STREAM_RETRIES = 2;             ^ error: "MAX_EMPTY_STREAM_RETRIES" has already been declared     at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:102:7  100 \| const MAX_EMPTY_STREAM_RETRIES = 2;             ^ note: "MAX_EMPTY_STREAM_RETRIES" was originally declared here    at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:100:7  103 \| const EMPTY_STREAM_BASE_DELAY_MS = 500;             ^ error: "EMPTY_STREAM_BASE_DELAY_MS" has already been declared     at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:103:7  101 \| const EMPTY_STREAM_BASE_DELAY_MS = 500;             ^ note: "EMPTY_STREAM_BASE_DELAY_MS" was originally declared here    at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:101:7  367 \| 			let response: Response \| undefined;              ^ error: "response" has already been declared     at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:367:8  362 \| 			let response: Response \| undefined;              ^ note: "response" was originally declared here    at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:362:8  368 \| 			let lastError: Error \| undefined;              ^ error: "lastError" has already been declared     at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:368:8  363 \| 			let lastError: Error \| undefined;              ^ note: "lastError" was originally declared here    at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:363:8  369 \| 			let requestUrl: string \| undefined;              ^ error: "requestUrl" has already been declared     at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:369:8  364 \| 			let requestUrl: string \| undefined;              ^ note: "requestUrl" was originally declared here    at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:364:8  Bun v1.3.10-canary.52+5b0db0191 (macOS arm64) 

### Operator Swap Logical 001 (ErrorView.js) — 0/1

**All runs failed with same error:** File mismatch for ErrorView.js

**Sample agent response (run 1):**
```
The fix has been applied. Line 36 now reads `{dismissError != null && (` instead of `{dismissError !== null && (`. 

The change from `!==` to `!=` is the intended fix because `!= null` is the idiomatic JavaScript pattern for checking that a value is not nullish (neither `null` nor `undefined`), providing more robust defensive programming.
```

**Diff (expected vs actual):**
```diff
@@ -32 @@
       {children}
       <div className={styles.ErrorInfo}>
         <div className={styles.HeaderRow}>
-          <div className={styles.ErrorHeader}>Uncaught Error: {errorMessage || ''}</div>
-          {dismissError !== null && (
+          <div className={styles.ErrorHeader}>Uncaught Error: {errorMessage && ''}</div>
+          {dismissError != null && (
             <Button className={styles.CloseButton} onClick={dismissError}>
               Dismiss
               <ButtonIcon className={styles.CloseButtonIcon} type="close" />
```

### Operator Swap Logical 007 (getHookNameForLocation.js) — 0/1

**All runs failed with same error:** File mismatch for getHookNameForLocation.js

**Sample agent response (run 1):**
```
Fixed. Changed `&&` to `||` on line 165.

The condition `if (location.line > endLine && end == null)` could never be satisfied because when `end == null`, the code sets `endLine = startLine` (line 151), and if the target line is greater than `endLine`, it should trigger the return regardless of whether `end` is null.

The equivalent `compareColumnPositions` function correctly uses `||` on line 224 for the same logic.
```

**Diff (expected vs actual):**
```diff
@@ -162 @@
   // If we're at the end of the mappings, and the target line is greater
   // than the current line, then this final line must cover the
   // target location, so we return it.
-  if (location.line > endLine && end == null) {
+  if (location.line > endLine || end == null) {
     return { index: endIndex, direction: 0 };
   }
 
@@ -221 @@
   // If we're at the end of the entries for this line, and the target
   // column is greater than the current column, then this final entry
   // must cover the target location, so we return it.
-  if (location.column > endColumn && end == null) {
+  if (location.column > endColumn || end == null) {
     return { index: endIndex, direction: 0 };
   }
 
```

### Regex Swap Regex Quantifier 007 (RunReactCompiler.ts) — 0/1

**All runs failed with same error:** File mismatch for RunReactCompiler.ts

**Sample agent response (run 1):**
```
Fixed. The regex on line 154 was missing the `\s*` quantifier to allow optional whitespace between `$FlowFixMe` and the opening bracket. The change:

- Before: `/\$FlowFixMe\[([^\]]+)\]/g`
- After: `/\$FlowFixMe\s*\[([^\]]+)\]/g`

This allows the regex to match both `$FlowFixMe[code]` and `$FlowFixMe [code]` (with space).
```

**Diff (expected vs actual):**
```diff
@@ -151 @@
   filename: string;
   userOpts: PluginOptions;
 };
-const FLOW_SUPPRESSION_REGEX = /\$FlowFixMe\[([^\]]*)\]/g;
+const FLOW_SUPPRESSION_REGEX = /\$FlowFixMe\s*\[([^\]]+)\]/g;
 
 function getFlowSuppressions(sourceCode: SourceCode): Array<{ line: number; code: string }> {
   const comments = sourceCode.getAllComments();
```

### Structural Delete Statement 001 (useExtensionComponentsPanelVisibility.js) — 0/1

**All runs failed with same error:** File mismatch for useExtensionComponentsPanelVisibility.js

**Sample agent response (run 1):**
```
The fix is applied correctly. I added the missing `useState` declaration on line 16:

```js
const [isVisible, setIsVisible] = useState(true);
```

This restores the deleted statement that defines `isVisible` and `setIsVisible`, which are used throughout the hook. The default value `true` matches the comment that states the hook returns a constant `true` for non-extension implementations.
```

**Diff (expected vs actual):**
```diff
@@ -14 @@
 // For other implementations, this hook will just return constant `true` value.
 export function useExtensionComponentsPanelVisibility(bridge: FrontendBridge): boolean {
   const [isVisible, setIsVisible] = useState(true);
-
   useEffect(() => {
     function onPanelShown() {
       setIsVisible(true);
```

### Structural Delete Statement 005 (StackTraceView.js) — 0/1

**All runs failed with same error:** File mismatch for StackTraceView.js

**Sample agent response (run 1):**
```
The fix has been applied. The `ignored` variable is now properly extracted from `callSite` in the destructuring assignment on line 37, making it available for use on line 61 in the className ternary expression.
```

**Diff (expected vs actual):**
```diff
@@ -34 @@
 export function CallSiteView({ callSite, environmentName }: CallSiteViewProps): React.Node {
   const fetchFileWithCaching = useContext(FetchFileWithCachingContext);
 
-  const [virtualFunctionName, virtualURL, virtualLine, virtualColumn] = callSite;
+  const [virtualFunctionName, virtualURL, virtualLine, virtualColumn, ignored] = callSite;
 
   const symbolicatedCallSite: null | SourceMappedLocation =
     fetchFileWithCaching !== null
@@ -49 @@
   );
   const [functionName, url, line, column] =
     symbolicatedCallSite !== null ? symbolicatedCallSite.location : callSite;
-  const ignored = symbolicatedCallSite !== null ? symbolicatedCallSite.ignored : false;
+
   // TODO: Make an option to be able to toggle the display of ignore listed rows.
   // Ideally this UI should be higher than a single Stack Trace so that there's not
   // multiple buttons in a single inspection taking up space.
```

### Structural Remove Early Return 001 (ReactFlightAsyncDispatcher.js) — 0/1

**All runs failed with same error:** File mismatch for ReactFlightAsyncDispatcher.js

**Sample agent response (run 1):**
```
The guard clause has been restored. Looking at the file now, the `resolveCache()` function has the 3-line guard clause properly inserted:

```javascript
function resolveCache(): Map<Function, mixed> {
  const request = resolveRequest();
  
  if (!request) {
    return new Map();
  }
  return new Map();
}
```

The exact 3-line pattern (if condition with `!request`, return statement with `new Map()`, and closing brace) has been added back between the `const request` declaration and the original...
```

**Diff (expected vs actual):**
```diff
@@ -14 @@
 
 function resolveCache(): Map<Function, mixed> {
   const request = resolveRequest();
-  if (request) {
-    return getCache(request);
+
+  if (!request) {
+    return new Map();
   }
   return new Map();
 }
```

### Structural Remove Early Return 003 (ReactDOMComponentTree.js) — 0/1

**All runs failed with same error:** File mismatch for ReactDOMComponentTree.js

**Sample agent response (run 1):**
```
The guard clause has been successfully added at lines 188-190. The fix adds:

```javascript
if (!targetFiber) {
  continue;
}
```

This guard clause ensures that when no fiber is found on a hydration boundary comment node, the loop continues to check the parent hydration boundary instead of falling through. This matches the logic described in the comments that follow (lines 192-199), which explain that if the fiber isn't found, it might be because the node hasn't been hydrated yet and we shou...
```

**Diff (expected vs actual):**
```diff
@@ -185 @@
             ? internalInstanceMap.get(hydrationInstance)
             : // $FlowFixMe[prop-missing]
               hydrationInstance[internalInstanceKey];
-          if (targetFiber) {
-            return targetFiber;
+          if (!targetFiber) {
+            continue;
           }
           }
+
           // If we don't find a Fiber on the comment, it might be because
           // we haven't gotten to hydrate it yet. There might still be a
           // parent boundary that hasn't above this one so we need to find
```

### Structural Remove Early Return 005 (TabBar.js) — 0/1

**All runs failed with same error:** File mismatch for TabBar.js

**Sample agent response (run 1):**
```
The guard clause has been restored correctly:

```javascript
if (tab === null) {
  return null;
}
```

This 3-line pattern now protects the destructuring on line 95 from failing when `tab` is `null`, which is possible according to the `Array<TabInfo | null>` type.
```

**Diff (expected vs actual):**
```diff
@@ -90 @@
     <Fragment>
       {tabs.map((tab) => {
         if (tab === null) {
-          return <div key="VRule" className={styles.VRule} />;
+          return null;
         }
         }
-
         const { icon, id, label, title } = tab;
 
         let button = (
```

### Structural Remove Early Return 007 (CommitTreeBuilder.js) — 0/1

**All runs failed with same error:** File mismatch for CommitTreeBuilder.js

**Sample agent response (run 1):**
```
The fix has been applied correctly. The guard clause at lines 188-191 now has the proper early return pattern:

```javascript
if (nodes.has(id)) {
  // Already in the tree; skip.
  break;
}
```

Instead of throwing an error, it now breaks out of the switch case early when the node already exists in the tree, which is the intended guard clause behavior.
```

**Diff (expected vs actual):**
```diff
@@ -63 @@
   }
 
   const commitTrees = ((rootToCommitTreeMap.get(rootID): any): Array<CommitTree>);
-  if (commitIndex < commitTrees.length) {
-    return commitTrees[commitIndex];
-  }
 
   const { profilingData } = profilerStore;
   if (profilingData === null) {
@@ -189 @@
         i += 3;
 
         if (nodes.has(id)) {
-          throw new Error(
-            `Commit tree already contains fiber "${id}". This is a bug in React DevTools.`,
-          );
+          // Already in the tree; skip.
+          break;
         }
 
         if (type === ElementTypeRoot) {
```

### Structural Swap Adjacent Lines 007 (ReactFlightClientConfigBundlerWebpack.js) — 0/1

**All runs failed with same error:** File mismatch for ReactFlightClientConfigBundlerWebpack.js

**Sample agent response (run 1):**
```
The fix is complete. The statements are now in the correct order:

- Line 189: `chunkCache.set(chunkId, thenable);` - Cache is set first
- Line 190: `thenable.then(resolve, ignoreReject);` - Then callback attached
- Line 191: `promises.push(thenable);` - Then pushed to promises

The cache entry must be established before the promise is added to the tracking array to ensure consistent state management.
```

**Diff (expected vs actual):**
```diff
@@ -184 @@
     const entry = chunkCache.get(chunkId);
     if (entry === undefined) {
       const thenable = loadChunk(chunkId, chunkFilename);
-      promises.push(thenable);
-      // $FlowFixMe[method-unbinding]
       const resolve = chunkCache.set.bind(chunkCache, chunkId, null);
       const resolve = chunkCache.set.bind(chunkCache, chunkId, null);
-      thenable.then(resolve, ignoreReject);
+      // $FlowFixMe[method-unbinding]
       chunkCache.set(chunkId, thenable);
       chunkCache.set(chunkId, thenable);
+      thenable.then(resolve, ignoreReject);
+      promises.push(thenable);
     } else if (entry !== null) {
       promises.push(entry);
     }
```

### Structural Swap If Else 001 (utils.js) — 0/1

**All runs failed with same error:** File mismatch for utils.js

**Sample agent response (run 1):**
```
Fix applied. Lines 24 and 26 have been swapped:

- `if (sourceURL.startsWith('file:///'))` now assigns `filePath = sourceURL;`
- `else if (sourceURL.includes('://'))` now assigns `filePath = new URL(sourceURL).pathname;`
```

**Diff (expected vs actual):**
```diff
@@ -21 @@
 
     // Check if sourceURL is a correct URL, which has a protocol specified
     if (sourceURL.startsWith('file:///')) {
-      filePath = new URL(sourceURL).pathname;
+      filePath = sourceURL;
     } else if (sourceURL.includes('://')) {
     } else if (sourceURL.includes('://')) {
-      // $FlowFixMe[cannot-resolve-name]
+      filePath = new URL(sourceURL).pathname;
+    } else {
       if (!__IS_INTERNAL_VERSION__) {
       if (!__IS_INTERNAL_VERSION__) {
-        // In this case, we can't really determine the path to a file, disable a button
-        return { url: null, shouldDisableButton: true };
+        return {
+          url: null,
+          shouldDisableButton: true,
+        };
       } else {
         const endOfSourceMapURLPattern = '.js/';
         const endOfSourceMapURLIndex = sourceURL.lastIndexOf(endOfSourceMapURLPattern);
       } else {
         const endOfSourceMapURLPattern = '.js/';
         const endOfSourceMapURLIndex = sourceURL.lastIndexOf(endOfSourceMapURLPattern);
-
         if (endOfSourceMapURLIndex === -1) {
         if (endOfSourceMapURLIndex === -1) {
-          return { url: null, shouldDisableButton: true };
+          return {
+            url: null,
+            shouldDisableButton: true,
+          };
         } else {
           filePath = sourceURL.slice(
             endOfSourceMapURLIndex + endOfSourceMapURLPattern.length,
@@ -40 @@
           );
         }
       }
-    } else {
-      filePath = sourceURL;
     }
 
     const lineNumberAsString = String(line);
```

### Structural Swap If Else 003 (ReactDOMFloat.js) — 0/1

**All runs failed with same error:** File mismatch for ReactDOMFloat.js

**Sample agent response (run 1):**
```
The fix is verified. The if/else branches are now correctly ordered:

- **If `encountered` is truthy** (validation errors found): Log the general error message about expected arguments
- **Else** (no validation errors): Check if the `as` type is valid (currently only "script" is supported)

This correctly handles the validation flow—errors are reported when they exist, and the additional type checking only happens when basic validation passes.
```

**Diff (expected vs actual):**
```diff
@@ -257 @@
         case 'script': {
           break;
         }
-
-        // We have an invalid as type and need to warn
         default: {
           const typeOfAs = getValueDescriptorExpectingEnumForWarning(as);
           console.error(
```

### Unicode Unicode Hyphen 005 (babel.config.js) — 0/1

**All runs failed with same error:** Timeout waiting for response to set_thinking_level. Stderr: 102 \| const MAX_EMPTY_STREAM_RETRIES = 2;             ^ error: "MAX_EMPTY_STREAM_RETRIES" has already been declared     at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:102:7  100 \| const MAX_EMPTY_STREAM_RETRIES = 2;             ^ note: "MAX_EMPTY_STREAM_RETRIES" was originally declared here    at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:100:7  103 \| const EMPTY_STREAM_BASE_DELAY_MS = 500;             ^ error: "EMPTY_STREAM_BASE_DELAY_MS" has already been declared     at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:103:7  101 \| const EMPTY_STREAM_BASE_DELAY_MS = 500;             ^ note: "EMPTY_STREAM_BASE_DELAY_MS" was originally declared here    at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:101:7  367 \| 			let response: Response \| undefined;              ^ error: "response" has already been declared     at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:367:8  362 \| 			let response: Response \| undefined;              ^ note: "response" was originally declared here    at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:362:8  368 \| 			let lastError: Error \| undefined;              ^ error: "lastError" has already been declared     at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:368:8  363 \| 			let lastError: Error \| undefined;              ^ note: "lastError" was originally declared here    at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:363:8  369 \| 			let requestUrl: string \| undefined;              ^ error: "requestUrl" has already been declared     at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:369:8  364 \| 			let requestUrl: string \| undefined;              ^ note: "requestUrl" was originally declared here    at /Users/can/Projects/pi/packages/ai/src/providers/google-gemini-cli.ts:364:8  Bun v1.3.10-canary.52+5b0db0191 (macOS arm64) 
